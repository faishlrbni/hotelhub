// useRooms Hook
// Managing room availability, room types, status overrides, and maintenance tickets.
// Awaiting Phase 2 implementation.

export function useRooms() {
  return {
    rooms: [],
    roomTypes: [],
    isLoading: true,
  };
}
